# Fast R-CNN

	1) 미리 학습된 CNN에 전체 이미지를 통과시켜 feature map을 추출
	2) selective search로 찾은 각각의 roi에 대해 roi pooling을 진행 -> 고정된 크기의 feature vector를 얻음
	3) feature vector는 fully connected layer를 통과한 뒤, 2개의 브랜치(softmax, bbox regressor)로 나뉨
	4-1) 하나의 브랜치는 softmax를 통과해 해당 roi가 어떤 물체인지 classification(SVM은 사용되지 않음) -> softmax 분류기
	4-2) 다른 브랜치를 bounding box regression을 통해 selective search로 찾은 박스의 위치를 조정 -> bbox regressor


### CNN
<img src="https://user-images.githubusercontent.com/33839093/109451994-1620e300-7a92-11eb-83f6-9bec6378d637.png" width="700">

- R-CNN : input image → selective search를 통해 2천개의 region proposal 뽑기 → CNN(2천번 수행)

- Fast R-CNN : input image → selective search를 통해 2천개의 region proposal 뽑기 → input image전체에 대해 CNN(1번 수행) → 뽑아놓은 roi영역을 feature map에 적용시키고, roi pooling을 통해 일정한 크기로 맞추기 → FCs → softmax & bbox regressor


### ROI pooling
> - FCs(fully-connected layer)에 넣기 위해서는 같은 크기의(resolution) feature map이 필요함
> - selective search를 통해 얻은 roi는 크기가 다르기 때문에 roi pooling을 수행함
> - 크기가 다른 feature map의 stride를 다르게, max pooling을 적용해 일정한 크기로 만듦
> ![fast_2](https://user-images.githubusercontent.com/33839093/109452200-90e9fe00-7a92-11eb-9b0b-e97dd0df9125.jpg)
> 👉 8x8크기의 feature map이 들어와 selective search로 뽑은 roi의 크기가 7x5일 때, output size를 2x2로 고정시키고 싶으면 stride=(3, 2)로 설정하면 됨
